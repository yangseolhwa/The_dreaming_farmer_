/*using UnityEngine;

public class AudioManager : MonoBehaviour
{
    private int volume = 80;  // 초기 볼륨 설정
    private bool isPlaying = false;

    public void SetVolume(int volume)
    {
        this.volume = volume;
    }

    public void PlayAudio()
    {
        isPlaying = true;
        // 여기에 실제 재생 로직을 넣는다
    }

    public void StopAudio()
    {
        isPlaying = false;
        // 여기에 정지 로직을 넣는다
    }

    public void SaveSettings()
    {
        // 설정 저장 로직을 넣는다
        Debug.Log($"볼륨 설정 저장됨: {volume}");
    }

    public void CancelSettings()
    {
        // 취소 시 볼륨 초기화
        volume = 80;
        Debug.Log("볼륨 설정 취소됨");
    }
}
*/